# GitHub PR Review Analysis - Environment Variables
# Copy this file to .env and fill in your values
# cp .env.example .env

# GitHub Authentication
# Required: Get your token from https://github.com/settings/tokens
# Required scopes: public_repo or repo
GITHUB_TOKEN=your_github_token_here

# JIRA Authentication
# Required: Get your API token from https://id.atlassian.com/manage-profile/security/api-tokens
JIRA_TOKEN=your_jira_api_token_here

# EvolutionIQ Email (shared across JIRA and Google Docs)
# Required: Your EvolutionIQ email address (same email used for JIRA and Google account)
EVOLUTIONIQ_EMAIL=your_email@evolutioniq.com

# JIRA Configuration
# Required: Your JIRA instance URL (e.g., https://yourcompany.atlassian.net)
JIRA_URL=https://yourcompany.atlassian.net

# Required: JIRA project key (e.g., WC, PROJ) - needed to prevent unbounded query errors
JIRA_PROJECT=WC

# Google Cloud Configuration
# Required: Your Google Cloud project ID
GOOGLE_CLOUD_PROJECT=eiq-development

# Optional: Vertex AI location (defaults to us-east4 if not set)
GOOGLE_CLOUD_LOCATION=us-east4

# Optional: Force Vertex AI mode (set automatically, but can override)
# GOOGLE_GENAI_USE_VERTEXAI=true

# Google Drive API (for gdocs-analyze)
# Note: Google Drive authentication uses OAuth 2.0 flow with credentials.json
# Place credentials.json in ~/.config/gdocs-analysis/credentials.json
# See gdocs-analysis/README.md for setup instructions
